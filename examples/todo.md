# Example todo list

Items the runner will work on, one per line with a `- [ ]` prefix.
Lines without a checkbox (like this heading) are ignored.

- [ ] Add a GET /healthz endpoint that returns 200 OK with body "ok"
- [ ] Add a CONTRIBUTING.md with sections for local setup, how to run tests, and how to open a PR
- [ ] Bump the Node version in package.json engines field to >=22
